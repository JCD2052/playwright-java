package org.jcd2052.core.elements.interfaces;

/**
 * Represents a text label or static text element on a web page.
 * <p>
 * This interface extends {@link IElement} and is used to identify elements
 * whose primary purpose is to display information (e.g., {@code <span>},
 * {@code <label>}, or heading tags) rather than to receive complex input.
 */
public interface ILabelElement extends IElement {
}